	INSTRUCTION FOR THE PROJECT

A. open the folder in command promt 

B. Create the Virtual Environment and activate it in main folder Sales data analyst		to create Virtual Environment run command python -m venv venv
	to activate it run command venv\Scripts\activate

C. Install the required libraries
	pip install streamlit 
	pip install pandas 
	pip install plotly 
	pip install matplotlib
	pip install openpyxl

D. Open the folder dashboard in command prompt 
E. run the command streamlit run app.py